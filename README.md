# backend-turnos-reservas
API REST de gestión de turnos y reservas construida con Node.js, Express y ESM. Arquitectura en capas (routes → controllers → services → repositories → DAO) con persistencia en archivos JSON, preparada para migrar a MongoDB con Mongoose en etapas posteriores.
